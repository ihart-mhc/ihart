import flash.display.MovieClip;//this function should be invoked when there's blob detectedprivate function shakeBall():void{	//declare instance ball	var ball: Ball;	//make a new ball type Ball	ball = new Ball();	//set property of the instance	ball.x = 278;	ball.x = 203;	//add to the screen	addChild(ball);	//assign move of ball	var ballMove: Number = 0.2;	//generate random move	var randomMove: int = Math.random()*10;		for (var i: int; i <100; i++)	{		//move the ball to the left, right, up, down		ball.GetPosition().x -= ballMove/2;		ball.GetPosition().x += ballMove;		ball.GetPosition().y -= ballMove;		ball.GetPosition().y += ballMove;	}}